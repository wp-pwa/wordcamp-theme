import React from 'react';
import styled from 'react-emotion';

const List = () => <Container>Favourites</Container>;

export default List;

const Container = styled.div`
  display: flex;
  justify-content: center;
`;
